/*
function concat(s1: string, s2: string): string
{
    return s1 + s2;
}

///////////////////////////////

interface HomeTaskData {
	howIDoIt: string;
	simeArray: (string|number)[];
}

interface HomeTask extends HomeTaskData{

	withData: HomeTaskData;
}

///////////////////////////////


interface MyArray<T> {
	[N: number]: T;

	reduce<U>(fn: (accum: U,  el: T) => U ) : U;
}*/
